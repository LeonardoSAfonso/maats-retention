# Copie para `.env` na mesma pasta (o `.env` está no .gitignore da raiz).
# Sem este arquivo a API sobe com os defaults, que batem com o docker compose.

DATABASE_URL=postgres://postgres:postgres@localhost:5432/smart_retention
PORT=3000
