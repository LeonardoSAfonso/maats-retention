# Copie para `.env` na mesma pasta (o `.env` está no .gitignore da raiz).
# NEXT_PUBLIC_* é embutido no bundle do browser, então precisa ser a URL pública da API.

NEXT_PUBLIC_API_URL=http://localhost:3000
